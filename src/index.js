// index.js
import React from "react";
import ReactDOM from "react-dom";
import App from "./app"; // Mengimpor elemen dari app.js

const app = <App / >;
ReactDOM.render(app, document.getElementById("root"));
